const { default: axios } = require('axios');
const express = require('express');
// const randomstring = require("randomstring");
const router = express.Router();


router.get('/' express.static(__dirname + '/view/'))

module.exports = router;